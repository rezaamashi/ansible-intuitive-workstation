# -*- mode: sh; -*-

# Additional command line arguments for xdg-desktop-portal-wlr(1).
OPTS='-r -l DEBUG'

# Setting this to 1 enables logging, any other value - disables.
LOGGING_ENABLE=1
# The directory will be created for you, if logging is enabled.
LOG_DIR="$HOME/.cache/log/xdg-desktop-portal-wlr"
